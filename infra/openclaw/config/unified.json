{
  "meta": {
    "description": "OpenClaw unified gateway: Daatan + Calendar agents, two Telegram bots"
  },
  "agents": {
    "defaults": {
      "workspace": "/workspace"
    },
    "list": [
      {
        "id": "daatan",
        "default": true,
        "name": "Daatan",
        "workspace": "/workspace/daatan",
        "identity": { "name": "Corvus", "theme": "Full-stack (Next.js, Prisma)", "emoji": "🦅" },
        "model": { "primary": "google/gemini-1.5-pro", "fallbacks": ["ollama/qwen:1.5b"] }
      },
      {
        "id": "calendar",
        "name": "Calendar",
        "workspace": "/workspace/year-shape",
        "identity": { "name": "Calendar Agent", "theme": "Year-shape (Vite, TypeScript)", "emoji": "📅" },
        "model": { "primary": "google/gemini-1.5-pro", "fallbacks": ["ollama/qwen:1.5b"] }
      }
    ]
  },
  "bindings": [
    { "agentId": "daatan", "match": { "channel": "telegram", "accountId": "daatan" } },
    { "agentId": "calendar", "match": { "channel": "telegram", "accountId": "calendar" } }
  ],
  "models": {
    "providers": {
      "ollama": {
        "baseUrl": "http://host.docker.internal:11434/v1",
        "apiKey": "ollama-local",
        "api": "openai-responses"
      }
    }
  },
  "channels": {
    "telegram": {
      "enabled": true,
      "dmPolicy": "pairing",
      "streamMode": "partial",
      "chatId": "${TELEGRAM_CHAT_ID}",
      "accounts": {
        "daatan": {
          "name": "Daatan",
          "botToken": "${TELEGRAM_BOT_TOKEN_DAATAN}"
        },
        "calendar": {
          "name": "Calendar",
          "botToken": "${TELEGRAM_BOT_TOKEN_CALENDAR}"
        }
      }
    }
  },
  "plugins": {
    "entries": {
      "telegram": { "enabled": true }
    }
  },
  "auth": {
    "profiles": {
      "google:default": { "provider": "google", "mode": "api_key" }
    }
  }
}
