# OpenClaw EC2 agents only. Do NOT mix with daatan app .env.
# Copy to .env, fill in values, run raise-openclaw.sh (it will scp .env to EC2).
# Two bots required: create both via @BotFather, one for Daatan, one for Calendar.

GEMINI_API_KEY=
TELEGRAM_CHAT_ID=
TELEGRAM_BOT_TOKEN_DAATAN=
TELEGRAM_BOT_TOKEN_CALENDAR=
