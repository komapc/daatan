#!/bin/bash
# Pre-commit hook: fast checks only (lint-staged + version bump check)
# Full build + tests run on pre-push

set -e

echo "🔍 Running pre-commit checks..."

# Check version bump for feature/fix branches
./scripts/check-version-bump.sh || exit 1

# Lint only staged files (fast)
echo "🔍 Running lint-staged..."
npx lint-staged

echo "✅ Pre-commit checks passed!"
